# TODO

- [ ] "How I Work" section needs a design pass before it goes back on the site.
      Currently hidden (commented out) in `app/page.tsx` — the content (`howSteps`),
      section styles (`app/styles/_how.scss`), and section markup are still in place,
      just not rendered. Un-comment and re-wire the nav pill / `SECTIONS` entry once
      it's redesigned.
- [ ] Add more case studies. Currently 4 (`app/data/case-studies.ts`): Delta, Fidelity,
      Liberty, YouFit.
- [ ] Rethink the About section stats (`app/page.tsx`, `.spec-sheet`) — currently
      "Projects shipped 40+ / Agency partners 12 / Team size 1, full stack".
- [ ] Add a picture of Darrough to the site (e.g. in the About section — currently
      text-only with a drop-cap, no photo).
- [ ] Add a logo (currently just the "DW" text badge in `.logo-mark` — header,
      favicon, and OG images all reuse this same circular text mark).
