import { Meta, Typeset } from "@storybook/addon-docs/blocks";

<Meta title="Foundations/Typography" />

# Typography

Three font families, loaded via Google Fonts in `app/layout.tsx`. Families,
weights, and the type scale below are generated from
[`tokens/typography.json`](https://github.com/darroughw/freelancer/blob/main/tokens/typography.json)
via [Style Dictionary](https://styledictionary.com) (`npm run build:tokens`)
— see [Foundations → Colors](?path=/docs/foundations-colors--docs) for how
that pipeline works. The weight and scale tokens aren't consumed by any
`app/styles/*.scss` partial yet (those still hardcode `font-weight`/`font-size`
per rule), but are available as `$weight-*`/`$scale-*` Sass variables,
`--weight-*`/`--scale-*` CSS custom properties, and `tokens.typography.*` in
TS for future use.

<table>
  <thead>
    <tr><th>Token path (JSON)</th><th>Sass</th><th>CSS custom property</th><th>TS</th></tr>
  </thead>
  <tbody>
    <tr><td><code>typography.font.paper</code></td><td><code>$paper-font</code></td><td><code>--font-paper</code></td><td><code>tokens.typography.font.paper</code></td></tr>
    <tr><td><code>typography.font.head</code></td><td><code>$head-font</code></td><td><code>--font-head</code></td><td><code>tokens.typography.font.head</code></td></tr>
    <tr><td><code>typography.font.mono</code></td><td><code>$mono-font</code></td><td><code>--font-mono</code></td><td><code>tokens.typography.font.mono</code></td></tr>
    <tr><td><code>typography.weight.paper-regular</code></td><td><code>$weight-paper-regular</code></td><td><code>--weight-paper-regular</code></td><td><code>tokens.typography.weight.paperRegular</code></td></tr>
    <tr><td><code>typography.weight.paper-bold</code></td><td><code>$weight-paper-bold</code></td><td><code>--weight-paper-bold</code></td><td><code>tokens.typography.weight.paperBold</code></td></tr>
    <tr><td><code>typography.weight.head</code></td><td><code>$weight-head</code></td><td><code>--weight-head</code></td><td><code>tokens.typography.weight.head</code></td></tr>
    <tr><td><code>typography.weight.mono</code></td><td><code>$weight-mono</code></td><td><code>--weight-mono</code></td><td><code>tokens.typography.weight.mono</code></td></tr>
    <tr><td><code>typography.scale.paper</code></td><td><code>$scale-paper</code></td><td><code>--scale-paper</code></td><td><code>tokens.typography.scale.paper</code></td></tr>
    <tr><td><code>typography.scale.head</code></td><td><code>$scale-head</code></td><td><code>--scale-head</code></td><td><code>tokens.typography.scale.head</code></td></tr>
    <tr><td><code>typography.scale.mono</code></td><td><code>$scale-mono</code></td><td><code>--scale-mono</code></td><td><code>tokens.typography.scale.mono</code></td></tr>
  </tbody>
</table>

## Paper — `$paper-font`

`'Archivo', -apple-system, sans-serif` — body copy, form fields, and most UI
text. Used at weights 400–800 depending on emphasis.

<Typeset
  fontFamily="'Archivo', -apple-system, sans-serif"
  fontWeight={400}
  fontSizes={[13, 15, 16]}
  sampleText="The quick brown fox jumps over the lazy dog."
/>

<Typeset
  fontFamily="'Archivo', -apple-system, sans-serif"
  fontWeight={700}
  fontSizes={[13, 15]}
  sampleText="The quick brown fox jumps over the lazy dog."
/>

## Head — `$head-font`

`'Archivo Black', 'Archivo', sans-serif` — display headings, always set at
weight 900. Sized fluidly with `clamp()` for large hero/section titles.

<Typeset
  fontFamily="'Archivo Black', 'Archivo', sans-serif"
  fontWeight={900}
  fontSizes={[16, 22, 28, 44, 64]}
  sampleText="Sample heading"
/>

## Mono — `$mono-font`

`'Space Mono', monospace` — section labels, meta text, tags, and tabular
data. Almost always uppercase and letter-spaced in context.

<Typeset
  fontFamily="'Space Mono', monospace"
  fontWeight={600}
  fontSizes={[10, 11, 12, 13]}
  sampleText="SECTION LABEL · META INFO"
/>

## Letter-spacing

Tracking is applied selectively, not as a blanket rule — it's what makes
small uppercase mono labels readable, so it clusters there:

<table>
  <thead>
    <tr><th>Value</th><th>Typical use</th></tr>
  </thead>
  <tbody>
    <tr><td><code>0.03em</code>–<code>0.05em</code></td><td>Body-adjacent labels (case-study meta, stat labels)</td></tr>
    <tr><td><code>0.08em</code>–<code>0.1em</code></td><td>Small uppercase mono labels (nav pills, section eyebrows, tags)</td></tr>
    <tr><td><code>-0.01em</code></td><td>One negative case: large display headings, where default tracking reads slightly loose at big sizes</td></tr>
  </tbody>
</table>
